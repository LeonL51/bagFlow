import 'package:flutter/material.dart';
 
class AuthSecondaryButtons extends StatelessWidget {
  const AuthSecondaryButtons({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}